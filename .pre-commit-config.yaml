repos:
  - repo: local
    hooks:
      - id: security-audit
        name: Multi-Agent Security Audit
        entry: python3 security_audit.py
        language: system
        pass_filenames: false
        always_run: false
        stages: [pre-commit]
        types: [python]
